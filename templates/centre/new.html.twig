{# templates/centre/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Créer un centre{% endblock %}

{% block body %}
<section class="p-6 bg-base-200 min-h-screen">
  <div class="max-w-2xl mx-auto">
    <div class="bg-base-100 rounded-2xl shadow p-6">

      {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
        {{ form_widget(form) }}

        <div class="flex justify-end gap-3 pt-2">
          <a href="{{ path('centre_index') }}" class="btn btn-ghost">Annuler</a>
          <button type="submit" class="btn btn-primary">
            Enregistrer
          </button>
        </div>
      {{ form_end(form) }}
    </div>
  </div>
</section>
{% endblock %}
